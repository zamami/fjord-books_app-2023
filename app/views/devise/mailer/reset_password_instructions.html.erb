<p>こんにちは <%= @resource.email %>!</p>

<p>パスワードを変更するためのリンクをリクエストされました。以下のリンクから変更できます。</p>

<p><%= link_to t('link.change_my_password'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしあなたがこのメールをリクエストしていないのであれば、無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、パスワードは変更されません。</p>
